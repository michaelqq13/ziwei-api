# nixpacks.toml

# Top-level assets config.
# Copies the local `rich_menu_images` directory to `rich_menu_images` inside the app's root directory (`/app`) in the container.
[assets]
"rich_menu_images" = "rich_menu_images"

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT" 